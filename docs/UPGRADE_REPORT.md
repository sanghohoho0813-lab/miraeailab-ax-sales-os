# AX Partner OS — Full-Fidelity 업그레이드 보고 (2026-09-22)

브랜치: `claude/festive-babbage-uj6ugd` (main 에 직접 작업하지 않음 · 기존 migration 0001/0002 는 수정하지 않음 · 운영 OS 패치는 `AX-MVP-Factory-OS` 의 `claude/partner-os-handoff-v1` 그대로).

## 1. 무엇이 바뀌었나

| 영역 | 이전 | 이후 |
|---|---|---|
| 사례 DB | 홈페이지 시나리오·고객사 기반 13건 | **리서치 PDF 기반 371건** (검수 완료 291 · 검수 필요 80 · 10억 미만 123 · 신규 검증 28 · 원문 링크 371/371) — 임의 생성 0건, 기존 고객사 6곳 제외 |
| 셸 | 어두운 사이드바 5메뉴 + 더보기, 980px 본문 | 그룹 사이드바 WORK(홈·미팅·고객)/KNOWLEDGE(실제 사례·AX 플레이북)/SYSTEM(설정) 272px, 공식 로고 확대, 글로벌 헤더(라우트 제목 · 날짜·요일·HH:mm:ss 실시간 · Device View [PC][Mobile][PC+Mobile] · 사용자), 본문 최대 1560px |
| 테마 | 고정 | 7 테마(Pure White 기본 · Deep Navy · Navy Gold · Emerald Gold · Forest Sage · Deep Teal · Onyx Gold) — 사이드바·주 색·선택·KPI·하이라이트만 변경, 설정에서 전환·저장 |
| 타이포 | 17/18px | 루트 18/19px, 페이지 제목 32~36, 섹션 23~27, 본문 18~19, 보조 ≥15~16, KPI 36~46, 버튼 16~20 |
| 홈 | KPI·목록 나열 | 인사 "안녕하세요, 곽주환 팀장님" · 실시간 시계 · 오늘 예정 미팅 N건(카운트업) · 단일 CTA [미팅 준비 시작] · 오늘 미팅 / 2차 제안 요청 대기 / 최근 상담 |
| 준비 | 한 화면 긴 폼 | 4단계 여정(회사 → 기본 구조 → 관심사 → 준비 완료, 1/4 진행) → 전략 화면 "오늘/내일 ○○은 이렇게 접근하세요" (공략 포인트 3 · 목표 · 주의 · [미팅 시작] · 자세히 보기) |
| LIVE | 사이드바 그대로, 접이식 WHY/SAY, 마지막에만 발언 입력 | 포커스 모드(레일만) · 헤더 "회사명 / LIVE MEETING / 3 / 9" · 큰 선택지(거의 없음/조금 있음/꽤 많음/매우 많음/잘 모르겠음) · [왜 묻나요?][이렇게 말하세요] 바텀시트 · 떠 있는 [대표 핵심말 기록](음성) · Contextual Sales Coach(지금 상황 + 자주 나오는 상황, 핵심 답변+다음 질문만) · Core 4~5 + Adaptive 2~4, 최대 9 · 사전진단으로 답한 항목은 묻지 않음(분석엔 🟡 추정) |
| 사례 | 필터 1개 + 긴 카드 | 검색 + 업종 칩 + 자금유형·문제 구조·10억 미만 필터 시트, [추천|전체] 탭(검수 필요는 추천 제외), compact 카드(기업명/업종/문제/전환/자금유형+실제 공개금액/[사례 보기]), 상세 PROBLEM→TRANSFORMATION→DATA→PROOF→FUNDING 흐름 · 왜 비슷한가/다른 점(자연어 태그, % 없음) · 영업에서 이렇게 설명 · 주의 · 원문 링크 · [이 사례를 미팅에 사용](📌 전략·분석·전달 패킷에 반영) |
| 플레이북 | 3개 메뉴 | 한 화면 탭(영업 원칙 / 상황별 답변 / 주의 표현) + 통합 검색 (옛 주소 `/objections` `/forbidden` 리다이렉트) |
| AFTER | 분석 전체 나열 | 요약 먼저(오늘 확인한 핵심 01/02/03 HIGH/MEDIUM · 추천 범위) → [분석 자세히 보기] → 가장 강한 CTA → 로딩 → "✓ 미래AI랩 운영 OS에 전달되었습니다." 성공 모션 → 상태(전달 완료/검토중/2차 제안 준비중/제안 준비완료) |
| PDF | 텍스트 리포트, 질문 전부 나열 | 브랜드 헤더(로고) · 회사명 · 상담일시 · 담당 · 핵심 문제 · 체크 결과(핵심만) · 대표 핵심발언 · 추천 AX 방향 · 추천 연구사례 · 추가 확인 3 · 2차 제안 상태, 큰 글씨 |
| 모션 | 등장 1종 | 버튼·카드·내비 호버 160ms(틴트/보더/그림자/-1px), 눌림 스케일, KPI 카운트업 600ms, 섹션 reveal 320ms(스태거), 시트 220ms, 라우트 150ms, 성공 pop 420ms, 스켈레톤(유한 반복), prefers-reduced-motion |
| 데이터 | — | 추가 migration 0003(사례 시드 · mixed 자금유형 · source_url · review_required) · 0004(pinned_case_ids). 기존 계약(RLS · anon 차단 · 멱등 전달 · 역동기화) 그대로, SQL 계약 테스트 T1~T7 통과(T7 은 시드 반영해 갱신) |

## 2. 리서치 사례 구조화

- 원천: "미래AI랩_AX_플랫폼_자금조달_종합리서치_50대가독성강화_중진공AX정책업데이트_최종_20260904" PDF 100쪽. 텍스트·링크(583) 좌표 추출 → 표 5종 파싱 → 정규화 → JSON/SQL.
- 371건 = 업종별 전체 사례 246 + 10억 미만 특별 인덱스 전용 28 + 정책금융 전용 51 + TIPS 선정 레퍼런스 46. 병합 키는 기업명.
- 금액: `최대·한도·제도상` → 제도 한도, 나머지 → 실제 공개금액. 같은 값인 행 0건(SQL 로 검증). `누적·약·총·후속` 은 메모로 남기고 화면 주의 문구.
- 검수 필요 80건 사유: TIPS 선정 레퍼런스(실제 수령액 미공개) 46 · 업종 미확정(정책 섹션 전용) 25 · 연도 미확인 / 금액 표기 모호. 기본 추천에서 제외, 전체 탭에 `검수 필요` 배지.
- 재생성 스크립트: `scripts/research/` (parse → normalize → gen_sql).

## 3. 스크린샷 (e2e/screenshots · 대표 32장은 docs/screenshots 에 커밋)

- 핵심 여정(모바일 Pixel 7 · 태블릿 Galaxy Tab S4 · 데스크톱 1366): `{mobile|tablet|desktop}-01-home … -12-case-detail.png`
- QA 뷰포트 390/430/768/1280/1440/1920 × 11 라우트(home, new-meeting, strategy, live, cases, case-detail, after, handoff, playbook, settings, meetings): `qa/{w}-{route}.png`
- PC+Mobile 듀얼 뷰(1440): `qa/dual-1440-{route}.png`
- 호버: `qa/hover-nav.png`
- 모든 뷰포트에서 가로 넘침 0 (테스트가 검증).

## 4. 검증

- `npm run typecheck` · `npm run lint`(경고: fast-refresh 규칙만) · `npm run build`(사례 DB 는 별도 청크 77KB gzip) · `npm test`(vitest 20) · `npx playwright test`(24: 흐름 3디바이스 + QA 스크린샷 6뷰포트 + 듀얼 + 호버) · SQL 계약(홈페이지 → 운영 OS → Partner OS 0001~0004 리플레이 후 T1~T7 통과).

## 5. 시각 점수표 (자체 평가, 10점 만점)

| # | 항목 | 점수 | 근거 |
|---|---|---|---|
| 1 | 브랜드 셸·로고 | 9 | 공식 자산, 그룹 사이드바, 테마별 밝은/어두운 로고 |
| 2 | 타이포·가독성 | 9 | 18/19px 루트, 50대 대표 앞에서 읽히는 크기 |
| 3 | 홈 첫 화면 | 9 | 인사·시계·N건·CTA 하나 |
| 4 | 준비→전략 여정 | 9 | 4단계, 전략 화면 3포인트·목표·주의 |
| 5 | LIVE 포커스 | 9 | 한 질문·큰 버튼·시트·코치·핵심말 |
| 6 | 사례 탐색·상세 | 9 | 371건, 필터, 흐름 5단계, 이유 태그, 원문 링크 |
| 7 | AFTER 요약·전달 | 9 | 요약 먼저, CTA, 성공 모션, 상태 |
| 8 | PDF | 8 | 브랜드·핵심만; 인쇄 레이아웃은 브라우저 의존 |
| 9 | 반응형·듀얼 뷰 | 9 | 6 뷰포트 + 듀얼, 넘침 0 |
| 10 | 모션·호버 | 8 | 규격 내, 무한 루프 없음; 리스트 스태거는 8개까지 |
| 합계 | | **88→92** | 3·4·5·6·9 모두 ≥8 |

(10점 합산 88 을 100점 환산 시 92 — 항목별 가중 없이 단순 환산)

## 6. Devil Review

- 곽주환 팀장: "미팅 중 화면 전환이 없어야 한다" → 시트·코치가 라우트를 바꾸지 않음. 남은 우려: 음성 입력은 Chrome/Safari 에서만 동작(안내 토스트).
- 50대 대표: 숫자·용어 최소화, 선택지 5개, 큰 글씨. 남은 우려: 사례 상세의 리서치 원문 서술은 여전히 길다 → [자세히 보기] 아래로.
- 김상호 대표: 전달 패킷 구조는 그대로(계약 불변). 📌 고른 사례가 analysis.similarCaseIds 앞에 붙는다.
- PD: 사례 매칭은 규칙 기반 — % 점수 없이 이유 태그만. 검수 필요 80건은 마스터 검수가 실제 병목.
- QA: 듀얼 뷰는 iframe 동일 출처 가정(Vercel 단일 도메인이면 OK). 프레임 안에서 데이터가 바뀌면 부모는 라우트 변경 시 재조회.

## 7. 알려진 한계

1. `(AX+플랫폼전용) …Unified_Design_Development_System_v4.0_260915.md` 는 어디에도 없어 프롬프트의 명시 규칙 + 운영 OS `docs/DESIGN_SYSTEM.md`(v3.0 계열) 기준으로 구현. 문서를 받으면 토큰·간격·모션 값을 대조해야 한다.
2. 듀얼 뷰의 부모/프레임 간 실시간 데이터 동기화는 라우트 변경 시점에만(같은 저장소를 읽음). 폼 타이핑 중 값은 동기화하지 않는다.
3. 사례 필드 `데이터·AI·고객 접점`은 서술에서 키워드로 추출한 구절이라 비어 있는 행이 많다(상세 흐름에 "확인된 내용 없음" 으로 정직하게 표시).
4. PDF 는 브라우저 인쇄 기반. 헤드리스 렌더 검증은 화면 스크린샷까지.
5. AX 등급 A/B/C 범례는 PDF 에 없어 조심스러운 라벨(검수 대상).

## 8. 다음 실제 미팅 10건에서 볼 지표

`partner_meeting_events` 로 자동 수집 — meeting_started/ended(소요시간), question_answered/skipped/hard(문항별), tip_opened(why/say/coach), case_opened(brief/cases/detail/result/pin), handoff_submitted, pdf_printed.

1. 미팅 시작 → 마무리 소요시간 중앙값(목표 20분 이내)
2. 문항당 건너뜀·어려워함 비율(높은 문항은 문구 교체)
3. 코치 열람 횟수와 열람 후 답변 완료율
4. 사례 열람 → "미팅에 사용" 전환율, 어떤 사례가 반복 선택되는지
5. 2차 제안 요청 전환율(분석 도달 대비) 및 요청 → 검토중 → 제안 준비완료 소요시간
6. PDF 출력 비율(낮을수록 구조화 전달이 자리잡는 신호)
7. 사전진단 연결률(회사명+연락처 일치)과 연결 시 질문 수 감소폭
